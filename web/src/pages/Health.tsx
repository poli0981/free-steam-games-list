import { PlaceholderPage } from "../components/common/QueryState";

export function HealthPage() {
  return (
    <PlaceholderPage
      title="Health"
      description="Dead-link checker, validation badges, and maintenance triggers — Phase 3."
    />
  );
}
