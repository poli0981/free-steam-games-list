{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-updater",
  "description": "Auto-updater permissions, and process:allow-restart so the app can relaunch once an update has installed. Desktop-only: the updater and process plugins are gated out of the Android/iOS build (see Cargo.toml + lib.rs), so this capability is restricted to desktop platforms to keep the mobile ACL resolution from failing on a non-existent permission.",
  "platforms": [
    "windows",
    "linux",
    "macOS"
  ],
  "windows": [
    "main"
  ],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
