{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window. Frontend is a static SPA fetching from raw.githubusercontent.com / api.github.com — only shell:open and updater are needed for native bridges.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "updater:default"
  ]
}
