# Tooling configuration only - this repo is not a pip-installable package.

[tool.vulture]
paths = ["scripts"]
# pytest collects test_* functions and fixtures by name; vulture cannot see that.
exclude = ["scripts/tests/"]
min_confidence = 80
